import { SearchingHotelPipe } from './searching-hotel.pipe';

describe('SearchingHotelPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchingHotelPipe();
    expect(pipe).toBeTruthy();
  });
});
